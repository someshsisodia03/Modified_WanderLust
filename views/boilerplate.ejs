<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WanderLust &mdash; Discover Your Next Stay</title>
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%23fe424d' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><circle cx='12' cy='12' r='10'/><polygon points='16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76' fill='%23fe424d'/></svg>">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css"
        integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.7.1/dist/leaflet.css" />
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800;900&family=Inter:wght@300;400;500;600;700;800&display=swap"
        rel="stylesheet">
    <style>
        /* â”€â”€â”€ Design Tokens â”€â”€â”€ */
        :root {
            --brand: #fe424d;
            --brand-dark: #d9333d;
            --brand-light: #fff0f1;
            --dark: #1a1a2e;
            --text: #374151;
            --muted: #6b7280;
            --border: #e5e7eb;
            --radius: 14px;
            --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.07);
            --shadow-md: 0 8px 24px rgba(0, 0, 0, 0.10);
            --shadow-lg: 0 16px 48px rgba(0, 0, 0, 0.14);
            --transition: 0.25s ease;
        }

        /* â”€â”€â”€ Base â”€â”€â”€ */
        *,
        *::before,
        *::after {
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            color: var(--text);
            background: #fafafa;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        main,
        body>div:not(.wl-footer):not(nav) {
            flex: 1;
        }

        /* â”€â”€â”€ Navbar â”€â”€â”€ */
        .navbar {
            background: rgba(255, 255, 255, 0.92) !important;
            backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--border) !important;
            padding: 0.75rem 1.5rem;
            box-shadow: var(--shadow-sm);
            margin-bottom: 0 !important;
        }

        .navbar-brand {
            font-family: 'Outfit', sans-serif;
            font-weight: 800;
            font-size: 1.5rem;
            letter-spacing: -0.5px;
            color: var(--brand) !important;
            display: flex;
            align-items: center;
            gap: 0.4rem;
        }

        .navbar-brand i {
            font-size: 1.3rem;
            color: var(--brand);
        }

        .nav-link {
            font-size: 0.95rem !important;
            font-weight: 500 !important;
            color: var(--text) !important;
            padding: 0.4rem 0.75rem !important;
            border-radius: 8px;
            transition: color 0.3s ease;
        }

        .nav-active {
            color: var(--brand) !important;
            font-weight: 600 !important;
        }

        /* ── Mobile nav dropdown styles ── */
        @media (max-width: 991.98px) {
            #navSlider {
                display: none !important;
            }

            #mainNavLinks .nav-link {
                padding: 0.65rem 0.75rem !important;
                border-radius: 8px;
                background: transparent !important;
            }

            #mainNavLinks .nav-link:hover {
                color: var(--brand) !important;
            }

            #mainNavLinks .nav-link.nav-active {
                color: var(--brand) !important;
                font-weight: 700 !important;
                background: transparent !important;
            }
        }


        .btn-search {
            background: linear-gradient(135deg, var(--brand), #ff6b6b);
            color: white;
            border: none;
            border-radius: 50px !important;
            padding: 0.5rem 1.5rem;
            font-weight: 600;
            font-size: 0.9rem;
            white-space: nowrap;
            flex-shrink: 0;
            transition: all var(--transition);
            box-shadow: 0 4px 14px rgba(254, 66, 77, 0.35);
        }

        .btn-search:hover {
            background: linear-gradient(135deg, var(--brand-dark), var(--brand));
            color: white;
            transform: translateY(-1px);
            box-shadow: 0 6px 20px rgba(254, 66, 77, 0.45);
        }

        /* Search form responsive container */
        .search-form {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            width: 100%;
            max-width: 520px;
        }

        .srch {
            border-radius: 50px !important;
            border: 1.5px solid var(--border);
            padding: 0.5rem 1.2rem;
            font-size: 0.9rem;
            flex: 1;
            min-width: 0;
            /* allow shrink below content width */
            transition: border var(--transition), box-shadow var(--transition);
        }

        .srch:focus {
            border-color: var(--brand);
            box-shadow: 0 0 0 3px rgba(254, 66, 77, 0.1);
            outline: none;
        }

        /* â”€â”€ Navbar search responsive â”€â”€ */
        .navbar-search-wrap {
            flex: 1;
            display: flex;
            justify-content: center;
            padding: 0 1rem;
        }

        .search-tabs-container {
            width: 100%;
            max-width: 540px;
        }

        /* â”€â”€ Search Tabs â”€â”€ */
        .search-tabs {
            display: flex;
            gap: 0.25rem;
            margin-bottom: 0.4rem;
        }

        .search-tab {
            display: flex;
            align-items: center;
            gap: 0.35rem;
            padding: 0.3rem 0.75rem;
            border: none;
            background: transparent;
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            border-radius: 50px;
            cursor: pointer;
            transition: all var(--transition);
            white-space: nowrap;
        }

        .search-tab i {
            font-size: 0.72rem;
        }

        .search-tab:hover {
            background: var(--brand-light);
            color: var(--brand);
        }

        .search-tab.active {
            background: var(--brand);
            color: white;
            box-shadow: 0 2px 8px rgba(254, 66, 77, 0.25);
        }

        /* â”€â”€ Search input with icon â”€â”€ */
        .search-input-wrap {
            position: relative;
            flex: 1;
            min-width: 0;
        }

        .search-input-wrap .search-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--muted);
            font-size: 0.85rem;
            pointer-events: none;
        }

        .search-input-wrap .srch {
            padding-left: 2.5rem;
        }

        @media (max-width: 991.98px) {
            .navbar-search-wrap {
                padding: 0.5rem 0;
                order: 3;
                width: 100%;
            }

            .search-tabs-container {
                max-width: 100%;
            }

            .search-form {
                max-width: 100%;
                width: 100%;
            }
        }

        @media (max-width: 576px) {
            .btn-search span {
                display: none;
            }

            /* hide text, keep icon */
            .btn-search {
                padding: 0.5rem 0.9rem;
            }

            .search-tab span {
                display: none;
            }

            .search-tab {
                padding: 0.3rem 0.6rem;
            }
        }

        /* â”€â”€â”€ Flash Alerts â”€â”€â”€ */
        .flash-container {
            position: fixed;
            top: 1.25rem;
            right: 1.25rem;
            z-index: 9999;
            display: flex;
            flex-direction: column;
            gap: 0.65rem;
            min-width: 280px;
            max-width: 380px;
        }

        .flash-toast {
            border-radius: 12px;
            padding: 0.9rem 1.1rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            box-shadow: var(--shadow-md);
            animation: toastIn 0.35s cubic-bezier(0.175, 0.885, 0.32, 1.275) both;
            font-size: 0.92rem;
            font-weight: 500;
        }

        .flash-toast.success {
            background: #ecfdf5;
            border-left: 4px solid #10b981;
            color: #065f46;
        }

        .flash-toast.danger {
            background: #fff1f2;
            border-left: 4px solid var(--brand);
            color: #9f1239;
        }

        .flash-toast i {
            font-size: 1.1rem;
            flex-shrink: 0;
        }

        .flash-toast .btn-close {
            margin-left: auto;
            font-size: 0.75rem;
        }

        @keyframes toastIn {
            from {
                opacity: 0;
                transform: translateX(30px) scale(0.9);
            }

            to {
                opacity: 1;
                transform: translateX(0) scale(1);
            }
        }

        /* â”€â”€â”€ Category Filters â”€â”€â”€ */
        .filters-wrap {
            position: relative;
            background: white;
            border-bottom: 1px solid var(--border);
            overflow: hidden;
        }

        /* Arrow buttons */
        .filter-arrow {
            display: none;
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            z-index: 3;
            width: 32px;
            height: 32px;
            border-radius: 50%;
            border: 1.5px solid var(--border);
            background: white;
            color: var(--text);
            font-size: 0.8rem;
            cursor: pointer;
            align-items: center;
            justify-content: center;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            transition: all 0.2s ease;
        }

        .filter-arrow:hover {
            background: var(--brand);
            color: white;
            border-color: var(--brand);
            box-shadow: 0 4px 12px rgba(254,66,77,0.3);
        }

        .filter-arrow-left { left: 4px; }
        .filter-arrow-right { right: 4px; }

        .filter-arrow.hidden { opacity: 0; pointer-events: none; }

        @media (max-width: 991.98px) {
            .filter-arrow {
                display: flex;
            }
        }

        .filters {
            display: flex;
            flex-wrap: nowrap;
            overflow-x: auto;
            gap: 0.25rem;
            padding: 1rem 1.5rem 0.5rem;
            background: white;
            scrollbar-width: none;
            -webkit-overflow-scrolling: touch;
            scroll-behavior: smooth;
        }

        @media (max-width: 991.98px) {
            .filters {
                padding: 1rem 40px 0.5rem;
                -webkit-mask-image: linear-gradient(to right, transparent 0px, black 42px, black calc(100% - 42px), transparent 100%);
                mask-image: linear-gradient(to right, transparent 0px, black 42px, black calc(100% - 42px), transparent 100%);
            }
        }

        .filters::-webkit-scrollbar {
            display: none;
        }

        .filter {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.3rem;
            padding: 0.6rem 1.1rem;
            border-radius: 12px;
            cursor: pointer;
            opacity: 0.6;
            transition: all var(--transition);
            white-space: nowrap;
            flex-shrink: 0;
            border-bottom: 2px solid transparent;
        }

        .filter:hover,
        .filter.active {
            opacity: 1;
            border-bottom-color: var(--brand);
        }

        .filter a {
            text-decoration: none;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.3rem;
        }

        .filter i {
            font-size: 1.4rem;
            color: var(--text);
        }

        .filter:hover i {
            color: var(--brand);
        }

        .icon-text {
            font-size: 0.72rem;
            font-weight: 600;
            color: var(--text);
            margin: 0;
        }

        /* â”€â”€â”€ Tax toggle â”€â”€â”€ */
        .tax-box {
            border: 1.5px solid var(--border);
            padding: 0.6rem 1rem;
            border-radius: 50px;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            white-space: nowrap;
            font-size: 0.85rem;
            font-weight: 500;
            flex-shrink: 0;
            background: white;
        }

        .tax-info {
            display: none;
        }

        @media (max-width: 991.98px) {
            .tax-box {
                display: none !important;
            }
        }

        /* â”€â”€â”€ Listing Cards â”€â”€â”€ */
        .cards {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(270px, 1fr));
            gap: 1.75rem;
            padding: 2rem 2.5rem;
        }

        .cardLink {
            text-decoration: none;
            color: inherit;
            display: block;
            border-radius: var(--radius);
            transition: transform var(--transition);
        }

        .cardLink:hover {
            transform: translateY(-4px);
        }

        .cardLink:hover .card {
            box-shadow: var(--shadow-lg);
        }

        .card {
            border: none !important;
            border-radius: var(--radius) !important;
            box-shadow: var(--shadow-sm);
            overflow: hidden;
            transition: box-shadow var(--transition);
            background: white;
        }

        .card-img-top {
            width: 100%;
            aspect-ratio: 16 / 9;
            height: auto;
            object-fit: cover;
            object-position: center;
            border-radius: 0 !important;
            transition: transform 0.4s ease;
            background: #1e293b;
        }

        .cardLink:hover .card-img-top {
            transform: scale(1.03);
        }

        .card-body {
            padding: 1rem 1.1rem 1.1rem;
        }

        .card-title-text {
            font-weight: 700;
            font-size: 1.08rem;
            color: var(--dark);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .card-location {
            font-size: 0.88rem;
            color: var(--muted);
            margin-top: 0.15rem;
        }

        .card-price {
            font-weight: 700;
            font-size: 0.95rem;
            color: var(--text);
            margin-top: 0.4rem;
        }

        .card-price span {
            font-weight: 400;
            color: var(--muted);
            font-size: 0.85rem;
        }

        /* â”€â”€â”€ Forms â”€â”€â”€ */
        .forms {
            display: flex;
            justify-content: center;
            padding: 2rem 1rem;
        }

        .wl-form-card {
            background: white;
            border-radius: 20px;
            box-shadow: var(--shadow-md);
            padding: 2.5rem 2.5rem;
            width: 100%;
            max-width: 580px;
        }

        .wl-form-title {
            font-size: 1.5rem;
            font-weight: 800;
            color: var(--dark);
            margin-bottom: 0.25rem;
        }

        .wl-form-subtitle {
            font-size: 0.9rem;
            color: var(--muted);
            margin-bottom: 1.75rem;
        }

        .form-label {
            font-weight: 600;
            font-size: 0.88rem;
            color: var(--text);
            margin-bottom: 0.3rem;
        }

        .form-control,
        .form-select {
            border: 1.5px solid var(--border);
            border-radius: 10px;
            padding: 0.6rem 0.9rem;
            font-size: 0.92rem;
            transition: border var(--transition), box-shadow var(--transition);
        }

        .form-control:focus,
        .form-select:focus {
            border-color: var(--brand);
            box-shadow: 0 0 0 3px rgba(254, 66, 77, 0.1);
            outline: none;
        }

        .btn-wl {
            background: linear-gradient(135deg, var(--brand), #ff6b6b);
            color: white;
            border: none;
            border-radius: 50px;
            padding: 0.65rem 2rem;
            font-weight: 700;
            font-size: 0.95rem;
            cursor: pointer;
            transition: all var(--transition);
            box-shadow: 0 4px 16px rgba(254, 66, 77, 0.35);
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-wl:hover {
            background: linear-gradient(135deg, var(--brand-dark), var(--brand));
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(254, 66, 77, 0.45);
            color: white;
        }

        .btn-wl-outline {
            background: transparent;
            color: var(--brand);
            border: 2px solid var(--brand);
            border-radius: 50px;
            padding: 0.55rem 1.6rem;
            font-weight: 600;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all var(--transition);
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.4rem;
        }

        .btn-wl-outline:hover {
            background: var(--brand);
            color: white;
            transform: translateY(-2px);
        }

        /* â”€â”€â”€ Detail page â”€â”€â”€ */
        .detail-wrapper {
            max-width: 860px;
            margin: 2rem auto;
            padding: 0 1.25rem;
        }

        .detail-img {
            display: block;
            width: 100%;
            min-width: 100%;
            aspect-ratio: 16 / 9;
            height: auto;
            object-fit: cover;
            object-position: center;
            border-radius: var(--radius);
            box-shadow: var(--shadow-md);
            background: #1e293b;
        }

        .detail-card {
            background: white;
            border-radius: var(--radius);
            box-shadow: var(--shadow-sm);
            padding: 1.75rem 2rem;
            margin-top: 1.5rem;
        }

        .detail-title {
            font-size: 1.7rem;
            font-weight: 800;
            color: var(--dark);
        }

        .detail-meta {
            color: var(--muted);
            font-size: 0.9rem;
            margin-bottom: 1rem;
        }

        .detail-desc {
            color: var(--text);
            font-size: 0.97rem;
            line-height: 1.7;
        }

        .detail-info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.6rem;
            margin-top: 1rem;
        }

        .detail-info-item {
            background: #f9fafb;
            border-radius: 10px;
            padding: 0.65rem 0.9rem;
            font-size: 0.88rem;
        }

        .detail-info-item strong {
            color: var(--dark);
            display: block;
            font-size: 0.76rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 0.1rem;
        }

        /* â”€â”€â”€ Review cards â”€â”€â”€ */
        .review-card {
            background: #f9fafb;
            border-radius: 12px;
            padding: 1rem 1.2rem;
            margin-bottom: 0.75rem;
            border: 1px solid var(--border);
        }

        .review-author {
            font-weight: 700;
            font-size: 0.92rem;
            color: var(--dark);
        }

        .review-comment {
            font-size: 0.9rem;
            color: var(--text);
            margin-top: 0.3rem;
            font-style: italic;
        }

        .btn-review-delete {
            background: #fee2e2;
            color: #b91c1c;
            border: none;
            border-radius: 8px;
            padding: 0.3rem 0.8rem;
            font-size: 0.8rem;
            font-weight: 600;
            cursor: pointer;
            transition: background var(--transition);
            margin-top: 0.5rem;
        }

        .btn-review-delete:hover {
            background: #fecaca;
        }

        .details {
            font-family: 'Inter', sans-serif;
        }

        /* â”€â”€â”€ Starability â”€â”€â”€ */
        .starability-result {
            position: relative;
            width: 150px;
            height: 30px;
            background-image: url("data:image/png;base64,");
            font-size: 0.1em;
            color: transparent;
        }

        .starability-result:after {
            content: ' ';
            position: absolute;
            left: 0;
            height: 30px;
            background-image: url("data:image/png;base64,");
            background-position: 0 -30px;
        }

        .starability-result[data-rating="5"]::after {
            width: 150px;
        }

        .starability-result[data-rating="4"]::after {
            width: 120px;
        }

        .starability-result[data-rating="3"]::after {
            width: 90px;
        }

        .starability-result[data-rating="2"]::after {
            width: 60px;
        }

        .starability-result[data-rating="1"]::after {
            width: 30px;
        }

        .starability-slot {
            display: block;
            position: relative;
            width: 150px;
            min-height: 60px;
            padding: 0;
            border: none;
        }

        .starability-slot>input {
            position: absolute;
            margin-right: -100%;
            opacity: 0;
        }

        .starability-slot>input:checked~label,
        .starability-slot>input:focus~label {
            background-position: 0 0;
        }

        .starability-slot>input:checked+label,
        .starability-slot>input:focus+label {
            background-position: 0 -30px;
        }

        .starability-slot>input[disabled]:hover+label {
            cursor: default;
        }

        .starability-slot>input:not([disabled]):hover~label {
            background-position: 0 0;
        }

        .starability-slot>input:not([disabled]):hover+label {
            background-position: 0 -30px;
        }

        .starability-slot>input:not([disabled]):hover+label::before {
            opacity: 1;
        }

        .starability-slot>input:focus+label {
            outline: 1px dotted #999;
        }

        .starability-slot .starability-focus-ring {
            position: absolute;
            left: 0;
            width: 100%;
            height: 30px;
            outline: 2px dotted #999;
            pointer-events: none;
            opacity: 0;
        }

        .starability-slot>.input-no-rate:focus~.starability-focus-ring {
            opacity: 1;
        }

        .starability-slot>label {
            position: relative;
            display: inline-block;
            float: left;
            width: 30px;
            height: 30px;
            font-size: 0.1em;
            color: transparent;
            cursor: pointer;
            background-image: url("data:image/png;base64,");
            background-repeat: no-repeat;
            background-position: 0 -30px;
        }

        .starability-slot>label::before {
            content: '';
            position: absolute;
            display: block;
            height: 30px;
            background-image: url("data:image/png;base64,");
            background-position: 0 30px;
            pointer-events: none;
            opacity: 0;
        }

        .starability-slot>label:nth-of-type(5)::before {
            width: 120px;
            left: -120px;
        }

        .starability-slot>label:nth-of-type(4)::before {
            width: 90px;
            left: -90px;
        }

        .starability-slot>label:nth-of-type(3)::before {
            width: 60px;
            left: -60px;
        }

        .starability-slot>label:nth-of-type(2)::before {
            width: 30px;
            left: -30px;
        }

        .starability-slot>label:nth-of-type(1)::before {
            width: 0;
            left: 0;
        }

        .starability-slot>input:checked~label,
        .starability-slot>input:hover~label,
        .starability-slot>input:focus~label {
            transition: background-position .7s;
        }

        /* â”€â”€â”€ Map â”€â”€â”€ */
        .map-wrapper {
            border-radius: var(--radius);
            overflow: hidden;
            box-shadow: var(--shadow-sm);
        }

        #map {
            width: 100%;
            height: 400px;
        }

        /* â”€â”€â”€ Section title â”€â”€â”€ */
        .section-title {
            font-size: 1.2rem;
            font-weight: 800;
            color: var(--dark);
            margin: 1.5rem 0 1rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .section-title::after {
            content: '';
            flex: 1;
            height: 1px;
            background: var(--border);
        }
    </style>
</head>

<body>
    <%- include('../layout/navbar') %>
        <div class="flash-container">
            <% var _msg=locals.msg&&locals.msg.length?locals.msg:null %>
                <% var _msge=locals.msge&&locals.msge.length?locals.msge:null %>
                    <% var _del=locals.del&&locals.del.length?locals.del:null %>
                        <% var
                            _err=locals.err&&locals.err.length?(Array.isArray(locals.err)?locals.err[0]:locals.err):null
                            %>
                            <% var _review=locals.review&&locals.review.length?locals.review:null %>
                                <% var _reviewe=locals.reviewe&&locals.reviewe.length?locals.reviewe:null %>
                                    <% if(_msg){ %>
                                        <div class="flash-toast success" role="alert"><i
                                                class="fa-solid fa-circle-check"></i><span>
                                                <%=_msg%>
                                            </span><button type="button" class="btn-close" aria-label="Close"></button>
                                        </div>
                                        <% } %>
                                            <% if(_msge){ %>
                                                <div class="flash-toast success" role="alert"><i
                                                        class="fa-solid fa-circle-check"></i><span>
                                                        <%=_msge%>
                                                    </span><button type="button" class="btn-close"
                                                        aria-label="Close"></button></div>
                                                <% } %>
                                                    <% if(_del){ %>
                                                        <div class="flash-toast danger" role="alert"><i
                                                                class="fa-solid fa-trash"></i><span>
                                                                <%=_del%>
                                                            </span><button type="button" class="btn-close"
                                                                aria-label="Close"></button></div>
                                                        <% } %>
                                                            <% if(_err){ %>
                                                                <div class="flash-toast danger" role="alert"><i
                                                                        class="fa-solid fa-circle-exclamation"></i><span>
                                                                        <%=_err%>
                                                                    </span><button type="button" class="btn-close"
                                                                        aria-label="Close"></button></div>
                                                                <% } %>
                                                                    <% if(_review){ %>
                                                                        <div class="flash-toast success" role="alert"><i
                                                                                class="fa-solid fa-star"></i><span>
                                                                                <%=_review%>
                                                                            </span><button type="button"
                                                                                class="btn-close"
                                                                                aria-label="Close"></button></div>
                                                                        <% } %>
                                                                            <% if(_reviewe){ %>
                                                                                <div class="flash-toast danger"
                                                                                    role="alert"><i
                                                                                        class="fa-solid fa-trash"></i><span>
                                                                                        <%=_reviewe%>
                                                                                    </span><button type="button"
                                                                                        class="btn-close"
                                                                                        aria-label="Close"></button>
                                                                                </div>
                                                                                <% } %>
        </div>
        <%- body %>
            <%- include('../layout/footer') %>
                <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
                    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
                    crossorigin="anonymous"></script>
                <script>
                    // Fix: Force reload when page is restored from bfcache (back button)
                    // This ensures the navbar shows correct auth state after login/logout
                    window.addEventListener('pageshow', function(e) {
                        if (e.persisted) { window.location.reload(); }
                    });
                    (() => { 'use strict'; document.querySelectorAll('.needs-validation').forEach(f => { f.addEventListener('submit', e => { if (!f.checkValidity()) { e.preventDefault(); e.stopPropagation() } f.classList.add('was-validated') }, false) }) })();
                    document.querySelectorAll('.flash-toast .btn-close').forEach(b => { b.addEventListener('click', () => { const t = b.closest('.flash-toast'); if (t) { t.style.opacity = '0'; t.style.transform = 'translateX(30px)'; t.style.transition = 'all .25s'; setTimeout(() => t.remove(), 260) } }) });
                    document.querySelectorAll('.flash-toast').forEach(t => { setTimeout(() => { if (document.body.contains(t)) { t.style.opacity = '0'; t.style.transition = 'all .35s'; setTimeout(() => t.remove(), 360) } }, 5000) });

                    // Filter arrow scroll
                    document.querySelectorAll('.filters-wrap').forEach(wrap => {
                        const scroller = wrap.querySelector('.filters');
                        const leftBtn = wrap.querySelector('.filter-arrow-left');
                        const rightBtn = wrap.querySelector('.filter-arrow-right');
                        if (!scroller || !leftBtn || !rightBtn) return;

                        function updateArrows() {
                            const sl = scroller.scrollLeft;
                            const maxScroll = scroller.scrollWidth - scroller.clientWidth;
                            if (maxScroll <= 5) {
                                leftBtn.classList.add('hidden');
                                rightBtn.classList.add('hidden');
                                return;
                            }
                            if (sl <= 5) leftBtn.classList.add('hidden');
                            else leftBtn.classList.remove('hidden');
                            if (sl >= maxScroll - 5) rightBtn.classList.add('hidden');
                            else rightBtn.classList.remove('hidden');
                        }

                        leftBtn.addEventListener('click', () => {
                            scroller.scrollBy({ left: -200, behavior: 'smooth' });
                        });
                        rightBtn.addEventListener('click', () => {
                            scroller.scrollBy({ left: 200, behavior: 'smooth' });
                        });

                        scroller.addEventListener('scroll', updateArrows, { passive: true });
                        updateArrows();
                    });
                </script>

    <!-- ══════════════════════════════════════════
         WanderLust AI Agent (ReAct Loop + Tool Calling)
         ══════════════════════════════════════════ -->
    <style>
        /* ── Chat FAB Button ── */
        #wl-chat-fab {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            z-index: 10000;
            width: 58px;
            height: 58px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--brand), #ff6b6b);
            border: none;
            cursor: pointer;
            box-shadow: 0 8px 28px rgba(254, 66, 77, 0.45);
            display: flex;
            align-items: center;
            justify-content: center;
            transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.25s ease;
            color: white;
            font-size: 1.4rem;
        }
        #wl-chat-fab:hover {
            transform: scale(1.1) translateY(-2px);
            box-shadow: 0 12px 36px rgba(254, 66, 77, 0.55);
        }
        #wl-chat-fab .fab-icon-open  { display: flex; }
        #wl-chat-fab .fab-icon-close { display: none; }
        #wl-chat-fab.open .fab-icon-open  { display: none; }
        #wl-chat-fab.open .fab-icon-close { display: flex; }

        /* Pulse ring on FAB */
        #wl-chat-fab::before {
            content: '';
            position: absolute;
            inset: -4px;
            border-radius: 50%;
            background: rgba(254, 66, 77, 0.2);
            animation: chatPulse 2.5s ease infinite;
        }
        @keyframes chatPulse {
            0%   { transform: scale(1);    opacity: 0.7; }
            70%  { transform: scale(1.35); opacity: 0; }
            100% { transform: scale(1.35); opacity: 0; }
        }

        /* ── Chat Panel ── */
        #wl-chat-panel {
            position: fixed;
            bottom: 5.5rem;
            right: 2rem;
            z-index: 9999;
            width: 370px;
            max-width: calc(100vw - 2rem);
            height: 520px;
            max-height: calc(100vh - 8rem);
            min-width: 300px;
            min-height: 350px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.18), 0 4px 16px rgba(0,0,0,0.09);
            display: flex;
            flex-direction: column;
            overflow: hidden;
            transform: scale(0.92) translateY(12px);
            opacity: 0;
            pointer-events: none;
            transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), opacity 0.25s ease;
            transform-origin: bottom right;
        }
        #wl-chat-panel.open {
            transform: scale(1) translateY(0);
            opacity: 1;
            pointer-events: all;
        }
        /* Disable smooth animation while resizing so it feels instant */
        #wl-chat-panel.resizing {
            transition: none !important;
        }

        /* ── Resize Handle (top-left corner) ── */
        .chat-resize-handle {
            position: absolute;
            top: 0;
            left: 0;
            width: 20px;
            height: 20px;
            cursor: nw-resize;
            z-index: 10;
            border-top-left-radius: 20px;
        }
        .chat-resize-handle::after {
            content: '';
            position: absolute;
            top: 6px;
            left: 6px;
            width: 8px;
            height: 8px;
            border-top: 2px solid rgba(255,255,255,0.6);
            border-left: 2px solid rgba(255,255,255,0.6);
            border-radius: 2px 0 0 0;
            opacity: 0;
            transition: opacity 0.2s ease;
        }
        #wl-chat-panel:hover .chat-resize-handle::after {
            opacity: 1;
        }

        /* ── Chat Header ── */
        .chat-header {
            background: linear-gradient(135deg, #1a1a2e 0%, #2d1b3d 100%);
            padding: 1rem 1.25rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            flex-shrink: 0;
        }
        .chat-header-avatar {
            width: 38px;
            height: 38px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--brand), #ff6b6b);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1rem;
            color: white;
            flex-shrink: 0;
        }
        .chat-header-info { flex: 1; min-width: 0; }
        .chat-header-name {
            font-weight: 700;
            font-size: 0.95rem;
            color: white;
            font-family: 'Outfit', sans-serif;
        }
        .chat-header-status {
            font-size: 0.75rem;
            color: rgba(255,255,255,0.65);
            display: flex;
            align-items: center;
            gap: 0.3rem;
            margin-top: 1px;
        }
        .chat-status-dot {
            width: 7px; height: 7px;
            border-radius: 50%;
            background: #22c55e;
            animation: statusBlink 2s ease infinite;
        }
        @keyframes statusBlink {
            0%, 100% { opacity: 1; }
            50%       { opacity: 0.4; }
        }

        /* ── Messages Area ── */
        .chat-messages {
            flex: 1;
            overflow-y: auto;
            padding: 1rem;
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
            background: #f8f9fb;
            scroll-behavior: smooth;
        }
        .chat-messages::-webkit-scrollbar { width: 4px; }
        .chat-messages::-webkit-scrollbar-track { background: transparent; }
        .chat-messages::-webkit-scrollbar-thumb { background: var(--border); border-radius: 99px; }

        /* ── Message Bubbles ── */
        .chat-msg {
            display: flex;
            gap: 0.5rem;
            align-items: flex-end;
            animation: msgIn 0.3s cubic-bezier(0.34, 1.4, 0.64, 1) both;
        }
        @keyframes msgIn {
            from { opacity: 0; transform: translateY(10px) scale(0.96); }
            to   { opacity: 1; transform: translateY(0) scale(1); }
        }
        .chat-msg.user { flex-direction: row-reverse; }

        .msg-avatar {
            width: 28px; height: 28px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--brand), #ff6b6b);
            display: flex; align-items: center; justify-content: center;
            font-size: 0.7rem; color: white; flex-shrink: 0;
        }
        .chat-msg.user .msg-avatar {
            background: linear-gradient(135deg, #6366f1, #8b5cf6);
        }

        .msg-bubble {
            max-width: 78%;
            padding: 0.6rem 0.9rem;
            border-radius: 14px;
            font-size: 0.875rem;
            line-height: 1.55;
            color: var(--text);
            background: white;
            box-shadow: 0 1px 4px rgba(0,0,0,0.08);
            border-bottom-left-radius: 4px;
            word-wrap: break-word;
        }
        .chat-msg.user .msg-bubble {
            background: linear-gradient(135deg, var(--brand), #ff6b6b);
            color: white;
            border-bottom-left-radius: 14px;
            border-bottom-right-radius: 4px;
            box-shadow: 0 4px 14px rgba(254,66,77,0.25);
        }

        /* ── Typing Indicator ── */
        .typing-indicator {
            display: flex; gap: 0.5rem; align-items: flex-end;
        }
        .typing-dots {
            background: white;
            border-radius: 14px;
            border-bottom-left-radius: 4px;
            padding: 0.65rem 0.9rem;
            box-shadow: 0 1px 4px rgba(0,0,0,0.08);
            display: flex; gap: 4px; align-items: center;
        }
        .typing-dots span {
            width: 7px; height: 7px;
            border-radius: 50%;
            background: var(--muted);
            animation: dotBounce 1.2s ease infinite;
        }
        .typing-dots span:nth-child(2) { animation-delay: 0.15s; }
        .typing-dots span:nth-child(3) { animation-delay: 0.30s; }
        @keyframes dotBounce {
            0%, 60%, 100% { transform: translateY(0); }
            30%            { transform: translateY(-6px); }
        }

        /* ── Suggested Prompts ── */
        .chat-suggestions {
            padding: 0.5rem 1rem 0;
            display: flex;
            flex-wrap: wrap;
            gap: 0.4rem;
        }
        .chat-suggestion-chip {
            background: white;
            border: 1.5px solid var(--border);
            border-radius: 50px;
            padding: 0.3rem 0.75rem;
            font-size: 0.75rem;
            color: var(--text);
            cursor: pointer;
            transition: all 0.2s ease;
            font-weight: 500;
            white-space: nowrap;
        }
        .chat-suggestion-chip:hover {
            border-color: var(--brand);
            color: var(--brand);
            background: var(--brand-light);
        }

        /* ── Input Area ── */
        .chat-input-area {
            padding: 0.75rem 1rem;
            border-top: 1px solid var(--border);
            background: white;
            display: flex;
            gap: 0.5rem;
            align-items: flex-end;
            flex-shrink: 0;
        }
        #wl-chat-input {
            flex: 1;
            border: 1.5px solid var(--border);
            border-radius: 12px;
            padding: 0.55rem 0.85rem;
            font-size: 0.875rem;
            font-family: 'Inter', sans-serif;
            resize: none;
            outline: none;
            max-height: 90px;
            transition: border-color 0.2s ease;
            line-height: 1.5;
        }
        #wl-chat-input:focus { border-color: var(--brand); }
        #wl-chat-send {
            width: 38px; height: 38px;
            border-radius: 10px;
            background: linear-gradient(135deg, var(--brand), #ff6b6b);
            border: none; cursor: pointer; color: white;
            display: flex; align-items: center; justify-content: center;
            font-size: 0.9rem; flex-shrink: 0;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            box-shadow: 0 3px 10px rgba(254,66,77,0.3);
        }
        #wl-chat-send:hover { transform: scale(1.08); box-shadow: 0 5px 16px rgba(254,66,77,0.4); }
        #wl-chat-send:disabled { opacity: 0.5; pointer-events: none; }

        /* ── RAG Badge ── */
        .rag-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
            font-size: 0.65rem;
            font-weight: 700;
            letter-spacing: 0.5px;
            text-transform: uppercase;
            color: rgba(255,255,255,0.5);
            padding: 0.2rem 0.5rem;
            background: rgba(255,255,255,0.08);
            border-radius: 50px;
        }

        /* ── Result Cards ── */
        .chat-cards {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
            margin-top: 0.6rem;
            width: 100%;
        }
        .chat-card {
            background: white;
            border: 1.5px solid var(--border);
            border-radius: 12px;
            padding: 0.7rem 0.9rem;
            display: flex;
            flex-direction: column;
            gap: 0.25rem;
            cursor: pointer;
            text-decoration: none;
            transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.15s ease;
            box-shadow: 0 1px 4px rgba(0,0,0,0.05);
        }
        .chat-card:hover {
            border-color: var(--brand);
            box-shadow: 0 4px 14px rgba(254,66,77,0.15);
            transform: translateY(-1px);
        }
        .chat-card-top {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 0.5rem;
        }
        .chat-card-title {
            font-weight: 700;
            font-size: 0.82rem;
            color: var(--dark);
            line-height: 1.3;
            flex: 1;
            min-width: 0;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }
        .chat-card-price {
            font-weight: 800;
            font-size: 0.78rem;
            color: white;
            background: linear-gradient(135deg, var(--brand), #ff6b6b);
            padding: 0.18rem 0.55rem;
            border-radius: 50px;
            white-space: nowrap;
            flex-shrink: 0;
        }
        .chat-card-meta {
            display: flex;
            align-items: center;
            gap: 0.4rem;
        }
        .chat-card-type {
            font-size: 0.68rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.4px;
            padding: 0.1rem 0.45rem;
            border-radius: 50px;
            flex-shrink: 0;
        }
        .chat-card-type.stay       { background: #dbeafe; color: #1d4ed8; }
        .chat-card-type.experience { background: #dcfce7; color: #166534; }
        .chat-card-type.destination{ background: #fef9c3; color: #92400e; }
        .chat-card-location {
            font-size: 0.73rem;
            color: var(--muted);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }
        .chat-card-arrow {
            font-size: 0.7rem;
            color: var(--brand);
            font-weight: 700;
            margin-left: auto;
            opacity: 0;
            transition: opacity 0.2s ease;
        }
        .chat-card:hover .chat-card-arrow { opacity: 1; }

        /* ── Agent Tool Trace ── */
        .agent-trace {
            margin-top: 0.6rem;
            border-top: 1px solid rgba(0,0,0,0.06);
            padding-top: 0.5rem;
        }
        .agent-trace-toggle {
            display: inline-flex;
            align-items: center;
            gap: 0.3rem;
            font-size: 0.7rem;
            font-weight: 600;
            color: #6366f1;
            cursor: pointer;
            background: #eef2ff;
            border: none;
            border-radius: 50px;
            padding: 0.2rem 0.6rem;
            transition: all 0.2s ease;
        }
        .agent-trace-toggle:hover { background: #e0e7ff; }
        .agent-trace-toggle i {
            transition: transform 0.2s ease;
            font-size: 0.6rem;
        }
        .agent-trace-toggle.expanded i { transform: rotate(180deg); }
        .agent-trace-steps {
            display: none;
            margin-top: 0.4rem;
            padding-left: 0.6rem;
            border-left: 2px solid #e0e7ff;
        }
        .agent-trace-steps.show { display: block; }
        .agent-step {
            padding: 0.35rem 0 0.35rem 0.6rem;
            font-size: 0.72rem;
            color: var(--text);
            position: relative;
            animation: stepIn 0.3s ease both;
        }
        .agent-step::before {
            content: '';
            position: absolute;
            left: -7px;
            top: 0.55rem;
            width: 10px;
            height: 10px;
            border-radius: 50%;
            background: linear-gradient(135deg, #6366f1, #8b5cf6);
            border: 2px solid white;
            box-shadow: 0 1px 3px rgba(99,102,241,0.3);
        }
        @keyframes stepIn {
            from { opacity: 0; transform: translateX(-8px); }
            to   { opacity: 1; transform: translateX(0); }
        }
        .agent-step-tool {
            font-weight: 700;
            color: #6366f1;
            font-family: 'Inter', monospace;
        }
        .agent-step-args {
            color: var(--muted);
            font-size: 0.68rem;
            font-style: italic;
        }
        .agent-step-result {
            color: #16a34a;
            font-size: 0.68rem;
            font-weight: 500;
        }
        .agent-step-time {
            color: var(--muted);
            font-size: 0.62rem;
            margin-left: 0.3rem;
        }
        /* ── Agent Badge ── */
        .agent-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
            font-size: 0.62rem;
            font-weight: 700;
            letter-spacing: 0.5px;
            text-transform: uppercase;
            color: rgba(255,255,255,0.55);
            padding: 0.15rem 0.5rem;
            background: rgba(255,255,255,0.08);
            border-radius: 50px;
        }

        @media (max-width: 480px) {
            #wl-chat-panel { right: 0.75rem; width: calc(100vw - 1.5rem); }
            #wl-chat-fab   { right: 1rem; bottom: 1.25rem; }
        }
    </style>


    <!-- ── Chat FAB Button ── -->
    <button id="wl-chat-fab" aria-label="Open AI Travel Assistant" title="Ask WanderLust AI">
        <span class="fab-icon-open"><i class="fa-solid fa-wand-magic-sparkles"></i></span>
        <span class="fab-icon-close"><i class="fa-solid fa-xmark"></i></span>
    </button>

    <!-- ── Chat Panel ── -->
    <div id="wl-chat-panel" role="dialog" aria-label="WanderLust AI Travel Assistant">
        <!-- Resize Handle -->
        <div class="chat-resize-handle" id="wl-chat-resize" title="Drag to resize"></div>
        <!-- Header -->
        <div class="chat-header">
            <div class="chat-header-avatar"><i class="fa-solid fa-robot"></i></div>
            <div class="chat-header-info">
                <div class="chat-header-name">WanderLust AI Agent</div>
                <div class="chat-header-status">
                    <span class="chat-status-dot"></span>
                    Online · ReAct Agent
                </div>
            </div>
            <span class="agent-badge"><i class="fa-solid fa-microchip"></i> Agent</span>
        </div>

        <!-- Messages -->
        <div class="chat-messages" id="wl-chat-messages">
            <!-- Welcome message from AI -->
            <div class="chat-msg bot" id="wl-welcome-msg">
                <div class="msg-avatar"><i class="fa-solid fa-robot"></i></div>
                <div class="msg-bubble">
                    Hey there! 🤖 I'm your <strong>AI Travel Agent</strong> — I autonomously search, compare, and analyze to find the best options for you.<br><br>
                    Try: <em>"Compare beach stays in Malibu"</em> or <em>"Plan a 3-day trip to Dubai"</em>
                </div>
            </div>
        </div>

        <!-- Suggested prompts (shown initially) -->
        <div class="chat-suggestions" id="wl-suggestions">
            <button class="chat-suggestion-chip" onclick="wlSendSuggestion(this)">🏖️ Beach stays under ₹5000</button>
            <button class="chat-suggestion-chip" onclick="wlSendSuggestion(this)">⚖️ Compare mountain stays</button>
            <button class="chat-suggestion-chip" onclick="wlSendSuggestion(this)">🗺️ Plan a trip to Dubai</button>
            <button class="chat-suggestion-chip" onclick="wlSendSuggestion(this)">⭐ Best reviewed experiences</button>
        </div>

        <!-- Input -->
        <div class="chat-input-area">
            <textarea
                id="wl-chat-input"
                placeholder="Ask about stays, experiences, destinations..."
                rows="1"
                aria-label="Chat message input"
            ></textarea>
            <button id="wl-chat-send" aria-label="Send message">
                <i class="fa-solid fa-paper-plane"></i>
            </button>
        </div>
    </div>

    <script>
    (function() {
        const fab      = document.getElementById('wl-chat-fab');
        const panel    = document.getElementById('wl-chat-panel');
        const messages = document.getElementById('wl-chat-messages');
        const input    = document.getElementById('wl-chat-input');
        const sendBtn  = document.getElementById('wl-chat-send');
        const suggsBox = document.getElementById('wl-suggestions');

        // ── Chat history for conversation memory ──
        // Stores the last N messages so follow-ups like "give me more" work
        const chatHistory = [];  // { role: 'user'|'bot', text: string }
        const MAX_HISTORY = 6;   // Keep last 6 messages (3 turns)

        function addToHistory(role, text) {
            chatHistory.push({ role, text: text.substring(0, 200) }); // cap length per msg
            // Keep only the last MAX_HISTORY messages
            while (chatHistory.length > MAX_HISTORY) chatHistory.shift();
        }

        // ── Toggle panel open/close ──
        fab.addEventListener('click', () => {
            const isOpen = panel.classList.toggle('open');
            fab.classList.toggle('open', isOpen);
            if (isOpen) input.focus();
        });

        // ── Auto-resize textarea ──
        input.addEventListener('input', () => {
            input.style.height = 'auto';
            input.style.height = Math.min(input.scrollHeight, 90) + 'px';
        });

        // ── Send on Enter (Shift+Enter = newline) ──
        input.addEventListener('keydown', (e) => {
            if (e.key === 'Enter' && !e.shiftKey) {
                e.preventDefault();
                sendMessage();
            }
        });
        sendBtn.addEventListener('click', sendMessage);

        // ── Suggestion chips ──
        window.wlSendSuggestion = function(btn) {
            input.value = btn.textContent.replace(/^[^\w\s₹]+\s*/, '').trim();
            suggsBox.style.display = 'none';
            sendMessage();
        };

        // ── Append a message bubble ──
        function appendMessage(text, role) {
            const isUser = role === 'user';
            const msgEl = document.createElement('div');
            msgEl.className = 'chat-msg ' + (isUser ? 'user' : 'bot');

            const iconHtml = isUser
                ? '<i class="fa-solid fa-user"></i>'
                : '<i class="fa-solid fa-robot"></i>';

            // Convert line breaks to <br> for bot messages
            const safeText = isUser
                ? escapeHtml(text)
                : escapeHtml(text).replace(/\n/g, '<br>').replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>');

            msgEl.innerHTML =
                '<div class="msg-avatar">' + iconHtml + '</div>' +
                '<div class="msg-bubble">' + safeText + '</div>';

            messages.appendChild(msgEl);
            messages.scrollTop = messages.scrollHeight;
            return msgEl;
        }

        // ── Show typing indicator ──
        function showTyping() {
            const el = document.createElement('div');
            el.className = 'typing-indicator';
            el.id = 'wl-typing';
            el.innerHTML =
                '<div class="msg-avatar"><i class="fa-solid fa-robot"></i></div>' +
                '<div class="typing-dots"><span></span><span></span><span></span></div>';
            messages.appendChild(el);
            messages.scrollTop = messages.scrollHeight;
        }

        function hideTyping() {
            const el = document.getElementById('wl-typing');
            if (el) el.remove();
        }

        // ── Escape HTML to prevent XSS ──
        function escapeHtml(str) {
            return String(str)
                .replace(/&/g, '&amp;')
                .replace(/</g, '&lt;')
                .replace(/>/g, '&gt;')
                .replace(/"/g, '&quot;');
        }

        // ── Core send function ──
        async function sendMessage() {
            const text = input.value.trim();
            if (!text) return;

            // Hide suggestion chips after first message
            suggsBox.style.display = 'none';

            // Show user bubble + track in history
            appendMessage(text, 'user');
            addToHistory('user', text);

            // Clear + reset input
            input.value = '';
            input.style.height = 'auto';

            // Disable send while waiting
            sendBtn.disabled = true;
            showTyping();

            try {
                const res = await fetch('/api/agent', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        message: text,
                        history: chatHistory.slice(0, -1)
                    })
                });
                const data = await res.json();
                hideTyping();

                const replyText = data.reply || "Sorry, I couldn't get a response. Please try again.";
                const msgEl = appendMessage(replyText, 'bot');
                addToHistory('bot', replyText);

                // Render agent tool trace (shows ReAct reasoning steps)
                if (data.toolTrace && data.toolTrace.length > 0) {
                    const bubble = msgEl.querySelector('.msg-bubble');
                    const traceHtml = buildToolTraceHtml(data.toolTrace);
                    bubble.insertAdjacentHTML('beforeend', traceHtml);
                }

                // Render visual cards if returned
                if (data.cards && data.cards.length > 0) {
                    const bubble = msgEl.querySelector('.msg-bubble');
                    const cardsHtml = buildCardsHtml(data.cards);
                    bubble.insertAdjacentHTML('beforeend', cardsHtml);
                }

            } catch (err) {
                hideTyping();
                appendMessage("Connection error. Please check your internet and try again.", 'bot');
            } finally {
                sendBtn.disabled = false;
                input.focus();
                messages.scrollTop = messages.scrollHeight;
            }
        }

        // ── Build HTML for recommendation cards ──
        function buildCardsHtml(cards) {
            if (!cards || cards.length === 0) return '';
            const cardItems = cards.map(card => {
                const typeLabel = card.type === 'stay' ? 'Stay'
                    : card.type === 'experience' ? 'Experience'
                    : 'Destination';

                const href = card.type === 'stay' ? `/moreabout/${card.id}`
                    : card.type === 'experience' ? `/experiences/${card.id}`
                    : `/destinations/${card.id}`;

                const locationHtml = card.location
                    ? `<i class="fa-solid fa-location-dot" style="font-size:0.65rem"></i> ${escapeHtml(card.location)}`
                    : '';

                const durationHtml = card.duration
                    ? ` &nbsp;·&nbsp; <i class="fa-regular fa-clock" style="font-size:0.65rem"></i> ${escapeHtml(card.duration)}`
                    : '';

                return `<a class="chat-card" href="${href}" target="_blank">
                    <div class="chat-card-top">
                        <span class="chat-card-title">${escapeHtml(card.title)}</span>
                        <span class="chat-card-price">${escapeHtml(card.priceLabel)}</span>
                    </div>
                    <div class="chat-card-meta">
                        <span class="chat-card-type ${card.type}">${typeLabel}</span>
                        <span class="chat-card-location">${locationHtml}${durationHtml}</span>
                        <span class="chat-card-arrow">View →</span>
                    </div>
                </a>`;
            }).join('');

            return `<div class="chat-cards">${cardItems}</div>`;
        }

        // ── Build HTML for agent tool trace (ReAct steps) ──
        function buildToolTraceHtml(trace) {
            if (!trace || trace.length === 0) return '';
            const traceId = 'trace-' + Date.now();
            const toolIcons = {
                search_stays: '🏨', search_experiences: '🎯',
                search_destinations: '🌍', get_listing_details: '📋',
                get_reviews: '⭐', compare_listings: '⚖️',
                find_similar: '🔗', plan_itinerary: '🗺️'
            };
            const steps = trace.map((step, i) => {
                const icon = toolIcons[step.tool] || '🔧';
                const argsStr = Object.entries(step.args || {})
                    .map(([k,v]) => k + '=' + escapeHtml(String(v)))
                    .join(', ');
                let resultStr = '';
                if (step.result) {
                    if (step.result.error) {
                        resultStr = '❌ ' + escapeHtml(step.result.error);
                    } else if (step.result.found !== undefined) {
                        resultStr = '✅ Found ' + step.result.found + ' results';
                    } else if (step.result.title) {
                        resultStr = '✅ ' + escapeHtml(step.result.title);
                    } else if (step.result.total !== undefined) {
                        resultStr = '✅ ' + step.result.total + ' reviews (avg: ' + (step.result.avg_score || 'N/A') + ')';
                    } else if (step.result.item1) {
                        resultStr = '✅ Compared: ' + escapeHtml(step.result.item1) + ' vs ' + escapeHtml(step.result.item2);
                    } else if (step.result.similar) {
                        resultStr = '✅ Found ' + step.result.similar.length + ' similar';
                    } else if (step.result.destination) {
                        resultStr = '✅ ' + escapeHtml(step.result.destination) + ' (' + step.result.days + ' days)';
                    } else {
                        resultStr = '✅ Done';
                    }
                }
                const timeStr = step.duration ? step.duration + 'ms' : '';
                return `<div class="agent-step" style="animation-delay:${i*0.1}s">
                    <span class="agent-step-tool">${icon} ${escapeHtml(step.tool)}</span>
                    ${argsStr ? '<br><span class="agent-step-args">' + argsStr + '</span>' : ''}
                    <br><span class="agent-step-result">${resultStr}</span>
                    <span class="agent-step-time">${timeStr}</span>
                </div>`;
            }).join('');

            return `<div class="agent-trace">
                <button class="agent-trace-toggle" onclick="(function(b){var s=b.nextElementSibling;s.classList.toggle('show');b.classList.toggle('expanded')})(this)">
                    <i class="fa-solid fa-microchip"></i> ${trace.length} tool${trace.length>1?'s':''} used
                    <i class="fa-solid fa-chevron-down"></i>
                </button>
                <div class="agent-trace-steps" id="${traceId}">${steps}</div>
            </div>`;
        }
    })();
    </script>

    <!-- ── Resize Script ── -->
    <script>
    (function() {
        const panel = document.getElementById('wl-chat-panel');
        const handle = document.getElementById('wl-chat-resize');
        if (!panel || !handle) return;

        const MIN_W = 300, MIN_H = 350;
        const MAX_W = 700, MAX_H = 800;
        let isResizing = false;
        let startX, startY, startW, startH;

        // Restore saved size from localStorage
        const savedW = localStorage.getItem('wlChatW');
        const savedH = localStorage.getItem('wlChatH');
        if (savedW) panel.style.width  = savedW + 'px';
        if (savedH) panel.style.height = savedH + 'px';

        handle.addEventListener('mousedown', onStart);
        handle.addEventListener('touchstart', onStart, { passive: false });

        function onStart(e) {
            e.preventDefault();
            e.stopPropagation();
            isResizing = true;
            panel.classList.add('resizing');

            const touch = e.touches ? e.touches[0] : e;
            startX = touch.clientX;
            startY = touch.clientY;
            startW = panel.offsetWidth;
            startH = panel.offsetHeight;

            document.addEventListener('mousemove', onMove);
            document.addEventListener('mouseup', onEnd);
            document.addEventListener('touchmove', onMove, { passive: false });
            document.addEventListener('touchend', onEnd);
            document.body.style.userSelect = 'none';
        }

        function onMove(e) {
            if (!isResizing) return;
            e.preventDefault();
            const touch = e.touches ? e.touches[0] : e;
            // Dragging top-left: moving left = wider, moving up = taller
            const dx = startX - touch.clientX;
            const dy = startY - touch.clientY;
            const newW = Math.min(MAX_W, Math.max(MIN_W, startW + dx));
            const newH = Math.min(MAX_H, Math.max(MIN_H, startH + dy));
            panel.style.width  = newW + 'px';
            panel.style.height = newH + 'px';
        }

        function onEnd() {
            if (!isResizing) return;
            isResizing = false;
            panel.classList.remove('resizing');
            document.removeEventListener('mousemove', onMove);
            document.removeEventListener('mouseup', onEnd);
            document.removeEventListener('touchmove', onMove);
            document.removeEventListener('touchend', onEnd);
            document.body.style.userSelect = '';

            // Persist size
            localStorage.setItem('wlChatW', panel.offsetWidth);
            localStorage.setItem('wlChatH', panel.offsetHeight);
        }
    })();
    </script>
</body>

</html>


